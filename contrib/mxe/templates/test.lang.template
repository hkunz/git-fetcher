/*
    Minimal test program for ${PACKAGE_NAME}, verifying basic functionality
    and compatibility with MXE static builds on Windows.

    To compile with MXE (example):

    ./usr/bin/${TARGET}-${COMPILER} \
        src/${PACKAGE_NAME}-test.${TEST_LANG} \
        -I usr/${TARGET}/include \
        -L usr/${TARGET}/lib \
        ${DEPENDENCIES} \
        -o usr/${TARGET}/bin/test-${PACKAGE_NAME}.exe
*/

${INCLUDES}

int main() {
    return 0;
}
